

class Device {
    constructor (config) {
        this.id = config.id
        this.image = config.image;
        this.title = config.title;
        this.text = config.text;
    }
}

export default Device;
